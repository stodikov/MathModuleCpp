#include <vector>
#include <iostream>

using namespace std;

#pragma once
class GeneralFunctions
{
public:
	static bool checkElementInVector(vector<int> elements, int element);
	static bool checkElementInVector(vector<string> elements, string element);
};

